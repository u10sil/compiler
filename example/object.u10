let model = Model {
	database: MySql {
		connection: Url "mysql://user:password@server.domain.tld/database"
		tables: {
			users: {
				columns: {
					id: { type: "number" indexed: true }
					name: { type: "string" }
				}
			}
		}
	}
	service: Rest {
		listen: Url "https://0.0.0.0:80/api"
		root: Resource {
			users: Collection {
				create: (data: s32) => data
				read: (id: u32) => model.database.tables.users.select(id)
				update: (id: u32, data: s32) => false
				delete: (id: u32) => false
			}
		}
	}
}
